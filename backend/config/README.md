# Backend Configuration

This directory contains configuration files for database, authentication, etc.

## TODO: Create config files for:

- `database.js` - MongoDB connection configuration
- `auth.js` - JWT and authentication settings
- `storage.js` - File upload/storage configuration
