# Backend Routes

This directory contains all API route handlers.

## TODO: Create the following routes:

- `auth.js` - Authentication routes (register, login, logout)
- `users.js` - User CRUD operations
- `subjects.js` - Subject and chapter management
- `quizzes.js` - Quiz and test routes
- `doubts.js` - Doubt forum routes
- `admin.js` - Admin panel routes

Each route file should follow RESTful API conventions.
